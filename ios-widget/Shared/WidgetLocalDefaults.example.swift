import Foundation

/// Copy to `WidgetLocalDefaults.swift` (gitignored) for local dev defaults.
enum WidgetLocalDefaults {
    static let baseURL = "http://127.0.0.1:8080"
    static let apiKey = ""
}
